{
  "name": "forward-ref",
  "version": "1.0.0",
  "description": "A Higher Order Component for React.forwardRef",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel src --out-dir lib --verbose --config-file ../../babel.config.js",
    "start": "npm run babel -- --watch",
    "build": "rm -rf dist && npm run babel",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wowlusitong/forward-ref.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wowlusitong/forward-ref/issues"
  },
  "homepage": "https://github.com/wowlusitong/forward-ref#readme",
  "peerDependencies": {
    "react": ">=16.3",
    "react-dom": ">=16.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "react": "^16.8.4",
    "react-dom": "^16.8.4"
  }
}
